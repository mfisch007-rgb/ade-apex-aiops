# Termux Execution Standard

Always execute from:

cd ~/ade-apex-aiops || exit 1

Use guarded payloads.

Generate complete subsystems.

Never manually assemble multiple AI outputs.

One payload.

One execution.

One validation.

One commit.
